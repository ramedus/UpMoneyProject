@model IEnumerable<UpMoneyProjesi.Models.SubscribeType>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SubscribeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SubscribeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SubscribeTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SubscribeTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SubscribeTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
